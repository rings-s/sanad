# Settings package. Set DJANGO_SETTINGS_MODULE to select an environment:
#   backend.settings.development  (default — SQLite, no external services)
#   backend.settings.production   (PostgreSQL, Redis, S3, Sentry)
